/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  // output: "export",
};

export default nextConfig;

// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   / config options here */
//   reactCompiler: true,
//   output: "export",
// };

// export default nextConfig;
